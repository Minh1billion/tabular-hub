[project]
name = "tabular-manner-app"
version = "0.1.0"
description = "MVP app: authentication and workspace management for Tabular Manner"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "alembic",
    "psycopg2-binary",
    "pydantic-settings",
    "authlib",
    "itsdangerous",
    "httpx",
    "python-jose[cryptography]",
    "redis",
    "python-multipart",
    "tabular-manner @ git+https://github.com/Minh1billion/tabular-manner.git@e3b15738627e28c29e6e3bb49219815aac36706e",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "fakeredis",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
asyncio_mode = "auto"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["app"]