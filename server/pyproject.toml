[project]
name = "tabular-manner-app"
version = "0.1.0"
description = "MVP app: authentication and workspace management for Tabular Manner"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "alembic",
    "psycopg2-binary",
    "pydantic-settings",
    "authlib",
    "itsdangerous",
    "httpx",
    "python-jose[cryptography]",
    "redis",
    "python-multipart",
    "boto3",
    "tabular-manner @ git+https://github.com/Minh1billion/tabular-manner.git@11d1148aa1e3552985e4d4a1ccf0b110eb1b9298",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "fakeredis",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"
asyncio_mode = "auto"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["app"]